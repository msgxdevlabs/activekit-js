---
---

Release configuration only: enter changesets pre mode (tag `alpha`) and
publish with `--tag next`, so prereleases ship without moving `latest` while
the API is still under construction. Releases nothing on its own.

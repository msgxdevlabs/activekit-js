---
---

Repository plumbing only, so nothing here releases: `automerge.yml` deletes the
head branch after the squash merge it just made, and `scripts/automerge-workflow.test.mjs`
drives that tail against a stub `gh`.

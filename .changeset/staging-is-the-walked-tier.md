---
---

Documentation only, so nothing here releases: staging is the only tier the
owner walks. `docs/short-owners.md` collapses the tier table onto staging and
marks dev as a session's own, and `CLAUDE.md` states the rule for a reader who
does not have `activekit-io`'s `LOOP.md`.

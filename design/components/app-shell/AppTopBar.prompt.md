Page header for product screens, paired with `AppSidebar`.

```jsx
<AppTopBar eyebrow="Workspace" title="Overview">
  <Button variant="secondary" size="sm">Export CSV</Button>
  <Button variant="primary" size="sm">New campaign</Button>
</AppTopBar>
```

Notes
- One filled teal pill maximum in the actions, same rule as marketing bands.
- `below` takes a tab row and switches the header to the tabbed padding.
- An intentional addition, not a DESIGN.md family; see readme "Intentional additions".
